lista =[]

while True :
    n = int(input('digite um numero da lista:'))
    if n not in lista:
        lista.append(n)
    r = str(input('deseja continuar?[S/N]'))
    if r in 'Nn':
        break
print (lista)
